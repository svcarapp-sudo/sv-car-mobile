import {usePartCategories as usePartCategoriesShared} from '@/global/hooks'

/** Re-export from shared. Parts feature uses shared hook for isolation. */
export const usePartCategories = usePartCategoriesShared
